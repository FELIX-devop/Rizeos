[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install --upgrade pip",
  ". /opt/venv/bin/activate && pip install -r requirements.txt",
  ". /opt/venv/bin/activate && pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl"
]

[start]
cmd = "bash -c 'export PYTHONUNBUFFERED=1 && . /opt/venv/bin/activate && echo \"=== Starting AI Service ===\" && echo \"PORT: $PORT\" && echo \"Python: $(python --version)\" && echo \"Starting uvicorn...\" && exec uvicorn app.main:app --host 0.0.0.0 --port $PORT --log-level info'"

